# create a repository in github

# you will get all commands in github after creating the repository, copy all the commands and execute them step by step


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
# Whenever you modify or make any changes the commands to be executed can be seen as follows

# git add .
# git commit -m "updated"
# git push origin 'branch-name'         [branch-name is what name your branch has]

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
# Creating branch

# two ways

# git branch "branch-name"
# git checkout "branch-name"

another way
# git checkout -b "branch-name"

then
# git pull origin main

# then Add -> Commit -> Push

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
# checking the status anytime

# git status